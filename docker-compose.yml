version: "3.9"
services:
  redis:
    image: "redislabs/redis-stack:edge"
    ports:
      - "6379:6379"
    volumes:
      - ./data:/data
    entrypoint: redis-server --appendonly yes
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure